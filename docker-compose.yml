version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: procraft_mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: procraft
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
